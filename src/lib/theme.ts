import { createTheme } from '@mui/system'

// Create a theme instance.
export const theme = createTheme({
  palette: {
    primary: {
      main: '#FFFFFF',
      dark: '#000000'
    },
    secondary: {
      main: '#D4D4D4'
    }
  }
})
