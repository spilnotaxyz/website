import { Story, Meta } from '@storybook/react'
import { Vision } from '@views'

export default {
  title: 'views/Vision',
  component: Vision
} as Meta

export const VisionView: Story = () => <Vision />
